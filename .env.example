# Frontend API base URL.
# Local development defaults to /api and uses the Vite proxy.
VITE_API_BASE_URL=/api

# Optional override for Vite dev proxy target.
# When omitted, Vite proxies /api to http://localhost:8000.
# VITE_BACKEND_PROXY_TARGET=http://localhost:8000
